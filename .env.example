# ════════════════════════════════════════════════
#  Bot_CR environment configuration template
#  Copy this file to .env and fill in your values.
#  .env is git-ignored - never commit real secrets.
# ════════════════════════════════════════════════

# ── Discord bot token (REQUIRED) ─────────────────────────────
# From https://discord.com/developers/applications -> your app -> Bot -> Token
BOT_TOKEN=CHANGE_ME_YOUR_BOT_TOKEN

# Command prefix (used side-by-side with slash commands)
PREFIX=!

# Server (guild) ID for instant slash-command sync.
# Leave empty to sync commands globally (can take up to an hour to propagate).
# Get your server ID: right-click your server icon -> Copy Server ID.
GUILD_ID=

# ── Appwrite data store (REQUIRED) ───────────────────────────
# Self-hosted Appwrite endpoint (keep the /v1 suffix).
APPWRITE_ENDPOINT=https://appwrite.alibks.dev/v1
# Project ID from Appwrite console -> Project Settings.
APPWRITE_PROJECT_ID=robotics-ops
# API key with databases read/write scopes (Appwrite console -> API Keys).
APPWRITE_API_KEY=
# Database that holds the bot collections (autocreated if missing).
APPWRITE_DATABASE_ID=robotics_ops

# ── Music ────────────────────────────────────────────────────
# Path to a Netscape-format youtube.com cookies.txt so /play and /queue auto
# can stream from datacenter IPs that YouTube bot-flags. Optional but strongly
# recommended. See "🍪 YouTube cookies for /play" in the README.
# YT_COOKIES_FILE=/path/to/cookies.txt

# ── Minecraft (Robotics CMC server bridge) ──────────────────
# Pterodactyl panel + Client API key (Account → API Credentials → Create).
# MC_SERVER_ID is the server's short identifier (e.g. 96f52139).
MC_PTERO_URL=https://panel.minecraft.bouyakhsass.com
MC_PTERO_CLIENT_KEY=
MC_SERVER_ID=
MC_ADDRESS=minecraft.alibks.dev
MC_PORT=25566
MC_SERVER_NAME=Robotics CMC
# Who may run /mcstart /mcstop /mcrestart — defaults to BOT_ADMIN_USER_IDS.
# Only these IDs + the Archon role; not pres/VP, staff roles or server admins.
# MC_CONTROL_USER_IDS=

# ── Channel names (channel @name or #name on the server) ─────
CHANNEL_RULES=•📚•-rules-of-the-server
CHANNEL_WELCOME=•👋•-joins
CHANNEL_GOODBYE=•👋•-leaves
CHANNEL_VERIFICATION=•📑•-verification
CHANNEL_ANNOUNCEMENTS=⦿announcements⦿
CHANNEL_DASHBOARD=⦿dashboard⦿
CHANNEL_BOTLOG=🤖bot-development

# ── Role names ───────────────────────────────────────────────
ROLE_TEMP=⛔ | None
ROLE_VERIFIED=「📗」Verified
ROLE_MEMBER=🌿 | LVL 01+

# Bot staff roles — holders can run every staff command (Archon, dev, ai bots).
# These must be the EXACT role display names on the club server (emoji prefixes
# included). Wrong or missing names silently downgrade staff to core-member
# access — the bot matches role names literally.
ROLE_ARCHON=「🏴」Archon of Robotics Club
ROLE_BOT_DEVELOPER=「👨💻」Bot Developer
ROLE_BOT_ADMIN=「👨💻」Server Developer

# Extra Discord user IDs (comma-separated) always treated as bot staff,
# regardless of roles — keeps the bot operator in control even during role
# reshuffles. Leave empty to disable.
BOT_ADMIN_USER_IDS=

# Leadership roles managed by /setlead (role names must match your server).
ROLE_PRESIDENT=「👸」President
ROLE_VICE_PRESIDENT=「👸」Vice President
ROLE_LEAD=「💻」Head of IT Unit copy
LEADER_ROLES=「👸」President,「👸」Vice President,「💻」Head of IT Unit copy

# ── Behaviour ────────────────────────────────────────────────
# Dashboard embed refresh interval (seconds).
DASHBOARD_REFRESH_SECONDS=60

# ── Club website (shown by /website) ─────────────────────────
WEBSITE_URL=https://robotics.ma

# ── KeepAlive web server port (free-host pings) ──────────────
PORT=8080

# ── Minecraft tagging / rally ────────────────────────────────
# Role auto-assigned to every linked Minecraft player (one @role mention pings
# the whole player base). Auto-created by the bot if it doesn't exist yet.
MC_PLAYER_ROLE=⛏️ Minecraft Player
# Min seconds between automatic "someone joined — jump in!" role pings.
MC_RALLY_COOLDOWN=2700

# ── mc-link (Discord ↔ Minecraft single sign-on) ─────────────
# AES-256-GCM key (32 bytes as hex) shared with the Paper plugin. Generate:
#   python -c "import secrets; print(secrets.token_hex(32))"
# Leave empty to disable the mc-link cog.
MC_LINK_SECRET=
# Link-code and temp-password lifetimes (seconds).
MC_LINK_CODE_TTL=300
MC_TEMP_TTL=300
# Watcher poll interval (seconds).
MC_LINK_POLL_SECONDS=5